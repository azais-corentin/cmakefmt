# cmakefmt: push { commentPreservation = "reflow", lineWidth = 40 }
# Here is an example:
# ```
# set(VERY_LONG_VARIABLE_NAME "this should not be reflowed at all")
# ```
# After the code block.
set(FOO "bar")
# cmakefmt: pop
