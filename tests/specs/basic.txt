== simple command preserved ==
project(MyProject)
[expect]
project(MyProject)

== multiple commands ==
project(MyProject)
cmake_minimum_required(VERSION 3.20)
[expect]
project(MyProject)
cmake_minimum_required(VERSION 3.20)

== command uppercased to lowercase ==
PROJECT(MyProject)
[expect]
project(MyProject)

== multiple args on one line ==
set(VAR a b c)
[expect]
set(VAR a b c)

== empty argument list ==
enable_testing()
[expect]
enable_testing()

== quoted argument ==
message(STATUS "hello world")
[expect]
message(STATUS "hello world")

== trailing whitespace stripped ==
project(MyProject)   
[expect]
project(MyProject)

== blank lines collapsed ==
project(MyProject)


cmake_minimum_required(VERSION 3.20)
[expect]
project(MyProject)

cmake_minimum_required(VERSION 3.20)
