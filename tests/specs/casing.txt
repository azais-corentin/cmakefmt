== command lower (default): uppercase command lowercased ==
PROJECT(Foo)
[expect]
project(Foo)

== command lower (default): mixed case lowercased ==
Add_Executable(app main.cpp)
[expect]
add_executable(app main.cpp)

== command lower (default): already lowercase unchanged ==
project(Foo)
[expect]
project(Foo)

== keyword upper (default): lowercase keywords uppercased ==
target_link_libraries(foo public bar private baz)
[expect]
target_link_libraries(foo PUBLIC bar PRIVATE baz)

== keyword upper (default): interface keyword uppercased ==
target_include_directories(foo interface include/)
[expect]
target_include_directories(foo INTERFACE include/)

== keyword upper (default): already uppercase unchanged ==
target_link_libraries(foo PUBLIC bar PRIVATE baz)
[expect]
target_link_libraries(foo PUBLIC bar PRIVATE baz)

== multiple keywords in one command ==
target_sources(mylib public include/mylib.h private src/mylib.cpp interface include/extra.h)
[expect]
target_sources(
  mylib
  PUBLIC
  include/mylib.h
  PRIVATE
  src/mylib.cpp
  INTERFACE
  include/extra.h
)
