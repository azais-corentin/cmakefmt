if(A)
if(B)
if(C)
if(D)
if(E)
if(F)
if(G)
if(H)
if(I)
if(J)
if(K)
if(L)
if(M)
if(N)
if(O)
if(P)
if(Q)
if(R)
if(S)
if(T)
message(STATUS "20 levels deep")
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
endif()
